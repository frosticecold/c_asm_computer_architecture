#include "asm.h"
#include <stdio.h>

int main() {
    char str1[20] = "password manhosa"; // dá mal se for utilizado o `
    int res;
    ptr1=str1;

    res = decrypt();
    printf("ptr1 desencriptada: \"%s\" caracteres alterados: %d\n", str1, res);
    
        

}
